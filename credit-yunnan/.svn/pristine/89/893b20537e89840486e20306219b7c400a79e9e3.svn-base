package com.ccb.portal.dao;

import java.util.List;

public interface BaseDao<T> {
	List<T> query(T obj);

	Integer add(T obj);

	Integer modi(T obj);

	Integer del(Integer id);

	T get(Integer id);
}
